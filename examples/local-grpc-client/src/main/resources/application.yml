server:
  port: 8181
spring:
  application:
    name: local-grpc-client

grpc:
  client:
    local-grpc-server:
      host:
        - 127.0.0.1
      port:
        - 9898
      enableKeepAlive: true
      keepAliveWithoutCalls: true